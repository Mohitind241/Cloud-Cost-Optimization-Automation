# S3 backend runtime configuration
# Injected via: terraform init -backend-config="tfstate.config"
# ⚠ Never commit real bucket names / account IDs to a public repo —
#   replace the values below with your own.

bucket         = "cloud-cost-opt-tfstate"          # Replace: your S3 bucket name
key            = "terraform/prod/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "cloud-cost-opt-tf-lock"           # Replace: your DynamoDB table name
encrypt        = true
